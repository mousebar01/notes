# Reference Agent Rules

这里保存外部资料原文。

- 不要直接大幅改写外部资料。
- 如果要总结外部资料，应另写到 `topics/` 或 `projects/` 的对应笔记中。
- 外部仓库放 `reference/repos/`。
- 当前不提前细分论文、官方文档和网页文章；零散外部资料先放在 `reference/` 下或按需要新建临时清晰目录。
- 等某一类资料明显变多，再拆出 `papers/`、`docs/`、`articles/` 这类子目录。
- 移动外部资料时注意不要破坏已有链接。
