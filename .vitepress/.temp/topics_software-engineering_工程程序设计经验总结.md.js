import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"工程程序设计经验总结","description":"","frontmatter":{},"headers":[],"relativePath":"topics/software-engineering/工程程序设计经验总结.md","filePath":"topics/software-engineering/工程程序设计经验总结.md"}');
const _sfc_main = { name: "topics/software-engineering/工程程序设计经验总结.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="工程程序设计经验总结" tabindex="-1">工程程序设计经验总结 <a class="header-anchor" href="#工程程序设计经验总结" aria-label="Permalink to &quot;工程程序设计经验总结&quot;">​</a></h1><p>这一路讨论下来，我有一个很重要的认知转变：</p><p>最开始看 Agent 代码时，我关注的是：</p><ul><li><code>ConversationEngine</code> 好在哪里？</li><li><code>TurnRunner</code> 好在哪里？</li><li>为什么这样拆？</li></ul><p>但后来慢慢发现，光看代码本身，其实很难判断好坏。</p><p>于是我们开始继续往下挖。</p><h2 id="_1-从-功能-到-职责" tabindex="-1">1. 从“功能”到“职责” <a class="header-anchor" href="#_1-从-功能-到-职责" aria-label="Permalink to &quot;1. 从“功能”到“职责”&quot;">​</a></h2><p>一开始看到：</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">class</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> ConversationEngine</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    ...</span></span></code></pre></div><p>第一反应会觉得：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>这不就是转发给 TurnRunner 吗？</span></span></code></pre></div><p>看不出精妙。</p><p>但往下拆后，才逐渐意识到每一层都在承担不同职责：</p><ul><li><code>ConversationEngine</code> = 会话入口</li><li><code>TurnRunner</code> = 单轮编排</li><li><code>ContextStore</code> = 上下文准备</li><li><code>ReactiveReasoner</code> = 模型工具循环</li><li><code>Phase</code> = 生命周期节点</li><li><code>Hook</code> = 扩展机制</li></ul><p>这时候才开始真正理解一件事：</p><blockquote><p>好架构首先不是“技巧高级”，而是职责边界清晰。</p></blockquote><h2 id="_2-从-代码-到-变化" tabindex="-1">2. 从“代码”到“变化” <a class="header-anchor" href="#_2-从-代码-到-变化" aria-label="Permalink to &quot;2. 从“代码”到“变化”&quot;">​</a></h2><p>后来我开始问：怎么判断代码写得好不好？</p><p>最直观的判断方式通常是看这些：</p><ul><li>命名</li><li>风格</li><li>目录结构</li></ul><p>但这些只能提供表面印象，不能真正说明质量。</p><p>真正的问题其实是：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>明天需求变了怎么办？</span></span></code></pre></div><p>比如：</p><ul><li>换模型</li><li>换 Memory</li><li>新增 Hook</li><li>新增 Tool</li><li>支持 MCP</li></ul><p>这时候才意识到：</p><blockquote><p>代码质量的本质，不是“现在能不能跑”，而是“变化时成本有多高”。</p></blockquote><h2 id="_3-从-变化-到-耦合" tabindex="-1">3. 从“变化”到“耦合” <a class="header-anchor" href="#_3-从-变化-到-耦合" aria-label="Permalink to &quot;3. 从“变化”到“耦合”&quot;">​</a></h2><p>再往下看，就会碰到一个更根本的问题：</p><ul><li>为什么有的代码容易改？</li><li>为什么有的代码一改全崩？</li></ul><p>根源通常就在耦合。</p><p>例如：</p><p>高耦合写法：</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">self</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.reasoner </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ReactiveReasoner()</span></span></code></pre></div><p>低耦合写法：</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">self</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.reasoner </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> deps.reasoner</span></span></code></pre></div><p>这里的区别不只是“写法不同”，而是设计上的可替换性完全不同。</p><p>于是就能串起这样一条链：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>低耦合</span></span>
<span class="line"><span>↓</span></span>
<span class="line"><span>易构造</span></span>
<span class="line"><span>↓</span></span>
<span class="line"><span>易测试</span></span>
<span class="line"><span>↓</span></span>
<span class="line"><span>易维护</span></span></code></pre></div><p>很多工程上的优点，其实不是彼此独立的，而是一环扣一环。</p><h2 id="_4-从-测试-到-设计" tabindex="-1">4. 从“测试”到“设计” <a class="header-anchor" href="#_4-从-测试-到-设计" aria-label="Permalink to &quot;4. 从“测试”到“设计”&quot;">​</a></h2><p>这是很关键的一步。</p><p>一开始我会觉得：测试是不是最难的部分？</p><p>后来慢慢发现，测试不是一个孤立技能。</p><p>测试本质上是在反向审视设计。</p><p>看到一个类时，不要先问：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>它是干什么的？</span></span></code></pre></div><p>而是先问：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>我怎么测试它？</span></span></code></pre></div><p>一旦这么问，很多设计问题会立刻暴露出来：</p><ul><li>输入是什么？</li><li>输出是什么？</li><li>依赖是什么？</li><li>边界在哪里？</li></ul><p>这时候才真正开始理解架构，而不只是“看懂代码流程”。</p><h2 id="_5-重新理解-边界" tabindex="-1">5. 重新理解“边界” <a class="header-anchor" href="#_5-重新理解-边界" aria-label="Permalink to &quot;5. 重新理解“边界”&quot;">​</a></h2><p>这是这次讨论里我最大的收获之一。</p><p>以前我对“边界”这个词的理解比较模糊，后来慢慢梳理出来：边界不是一个单一概念，至少有三层。</p><h3 id="_5-1-正常边界" tabindex="-1">5.1 正常边界 <a class="header-anchor" href="#_5-1-正常边界" aria-label="Permalink to &quot;5.1 正常边界&quot;">​</a></h3><p>最基础的一层是输入和输出。</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>输入</span></span>
<span class="line"><span>↓</span></span>
<span class="line"><span>输出</span></span></code></pre></div><p>例如：</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">ContextStore.prepare(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">...</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span></code></pre></div><p>输入消息，输出 <code>ContextBundle</code>。</p><h3 id="_5-2-异常边界" tabindex="-1">5.2 异常边界 <a class="header-anchor" href="#_5-2-异常边界" aria-label="Permalink to &quot;5.2 异常边界&quot;">​</a></h3><p>系统不只要处理正常情况，还要处理失败路径，例如：</p><ul><li>非法输入</li><li>Tool 失败</li><li>LLM 超时</li><li>Memory 崩溃</li></ul><h3 id="_5-3-变化边界" tabindex="-1">5.3 变化边界 <a class="header-anchor" href="#_5-3-变化边界" aria-label="Permalink to &quot;5.3 变化边界&quot;">​</a></h3><p>还有一层常常更重要：未来替换和扩展时，系统能不能扛住。</p><p>例如：</p><ul><li>换 Provider</li><li>换 Memory</li><li>换 ToolRegistry</li></ul><p>如果一换就全身联动，说明边界其实没有建立起来。</p><p>所以我最后得到一个更完整的定义：</p><blockquote><p>边界 = 模块对外的承诺 + 可能失败的地方 + 未来可能变化的地方。</p></blockquote><h2 id="_6-重新理解测试" tabindex="-1">6. 重新理解测试 <a class="header-anchor" href="#_6-重新理解测试" aria-label="Permalink to &quot;6. 重新理解测试&quot;">​</a></h2><p>后来再回头看测试代码，理解又变了。</p><p>一开始以为测试是在验证：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>功能是否正确</span></span></code></pre></div><p>后来发现这只说对了一部分。</p><p>更准确地说，测试是在保护边界。</p><p>例如：</p><ul><li><code>Foundation Types</code> 保护的是数据结构契约</li><li><code>ToolDiscoveryState</code> 保护的是状态管理规则</li><li><code>ReactiveReasoner</code> 保护的是模型工具循环</li><li><code>Lifecycle</code> 保护的是 Hook 扩展机制</li></ul><p>所以可以把这个认知浓缩成一句话：</p><blockquote><p>测试不只是测功能，测试是在保护设计。</p></blockquote><h2 id="_7-重新理解-工程-到底在做什么" tabindex="-1">7. 重新理解“工程”到底在做什么 <a class="header-anchor" href="#_7-重新理解-工程-到底在做什么" aria-label="Permalink to &quot;7. 重新理解“工程”到底在做什么&quot;">​</a></h2><p>最后一个很本质的问题是：</p><blockquote><p>工程上是不是不只是写正常流程？</p></blockquote><p>答案是肯定的。</p><p>甚至可以说：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>正常流程代码</span></span>
<span class="line"><span>可能只占 20%</span></span>
<span class="line"><span></span></span>
<span class="line"><span>剩下 80%</span></span>
<span class="line"><span>都在处理边界</span></span></code></pre></div><p>例如：</p><ul><li>用户输入错了怎么办？</li><li>模型超时怎么办？</li><li>模型乱调用工具怎么办？</li><li>工具失败怎么办？</li><li>工具无限循环怎么办？</li><li>上下文爆炸怎么办？</li><li>消息发送失败怎么办？</li><li>未来换模型怎么办？</li></ul><p>这些问题往往才是工程工作的主体，而不是“happy path 写完就结束”。</p><h2 id="最终总结" tabindex="-1">最终总结 <a class="header-anchor" href="#最终总结" aria-label="Permalink to &quot;最终总结&quot;">​</a></h2><p>如果把这次讨论压缩成一句话，那就是：</p><blockquote><p>Demo 的目标是让功能跑起来。</p><p>工程的目标是让系统在变化、失败、扩展、协作的情况下仍然稳定运行。</p></blockquote><p>所以以后再看 Agent 代码，不要先问：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>这个函数写得聪不聪明？</span></span></code></pre></div><p>而要先问：</p><ul><li>职责边界是否清晰？</li><li>依赖是否容易替换？</li><li>是否容易构造？</li><li>是否容易测试？</li><li>异常情况是否考虑？</li><li>未来变化是否容易支持？</li></ul><p>当开始用这些问题审视代码时，就已经在从“会写代码的人”，逐渐转向“会设计系统的人”。</p><p>而这恰恰也是目前 AI 最难稳定替代、最有工程价值的能力之一。</p><h2 id="一句话提醒自己" tabindex="-1">一句话提醒自己 <a class="header-anchor" href="#一句话提醒自己" aria-label="Permalink to &quot;一句话提醒自己&quot;">​</a></h2><p>以后判断一段代码，不只看它能不能跑。</p><p>更要看它在变化、失败、扩展和协作到来时，能不能依然稳得住。</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("topics/software-engineering/工程程序设计经验总结.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const __________ = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  __________ as default
};
